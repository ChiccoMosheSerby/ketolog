{
  "name": "ketolog",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "KetoLog — MERN keto diary (Express + MongoDB + React/Vite). Single-service deploy: the API also serves the built client.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build && npm --prefix server install",
    "start": "npm --prefix server start"
  }
}
